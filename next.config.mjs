/** @type {import('next').NextConfig} */
// const nextConfig = {};
const nextConfig = {
    images: {
      domains: ["dummyjson.com"],
      domains: ["images.pexels.com"],
    },
  };

export default nextConfig;
